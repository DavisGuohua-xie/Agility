import Immutable from "seamless-immutable";

export default Immutable({
    ajaxCallsInProgress: 0,
    logging_in: false,
    logged_in: false,
    user: null,
    errors: [],
    loggedIn: false
});
